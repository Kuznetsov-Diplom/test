version: '3.8'

services:
  biometric-demo:
    build:
      context: .
    ports:
      - "8501:8501"
    restart: unless-stopped

    # Для разработки можно добавить volumes:
    # volumes:
    #   - .:/app