services:
  biometric-demo:
    # Используем локальный образ, который собирается в CI
    image: biometric-demo:latest
    ports:
      - "8501:8501"
    restart: unless-stopped
    # Для разработки можно раскомментировать:
    # volumes:
    #   - .:/app
    #   - /app/__pycache__:/app/__pycache__